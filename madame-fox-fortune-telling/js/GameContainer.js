export class GameContainer {
    constructor(main) {
        const styles = 
        `
        .game-container {
            position: relative;
            padding: 16px;
        }
        `;
        
        main.addStyles(styles);
    }
}